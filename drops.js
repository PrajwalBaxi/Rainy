/*class drops {
    constructor(x,y,width, height) {
    }
  
    display() {
      var pos =this.body.position;
      rectMode(CENTER);
      strokeWeight(5);
      stroke("black");
      fill("blue");
      rect(pos.x, pos.y, this.width, this.height);
  //pop();
    }
  };
  */